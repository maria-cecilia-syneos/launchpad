import { WorkspaceSurfacePlaceholder } from "@/components/launchpad/WorkspaceSurfacePlaceholder";

export default function AdminPage() {
  return (
    <WorkspaceSurfacePlaceholder
      description="A role-permitted workspace surface for source systems, source records, ingestion status, source-link health, and access-aware provenance."
      title="Admin"
    />
  );
}
